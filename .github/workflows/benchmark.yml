name: Benchmark

on:
    workflow_dispatch:
    push:

concurrency:
  group: ${{ github.workflow }}-${{ github.sha }}
  cancel-in-progress: true

jobs:
  benchmark:
    name: Run benchmark
    runs-on: ubuntu-22.04
    timeout-minutes: 60
    strategy:
      fail-fast: false
    steps:
      - uses: actions/checkout@v2
      - run: make build
      - run: docker compose up -d && sleep 10
        timeout-minutes: 5
      - run: docker exec -it ishocon2-bench-1 ./benchmark --ip app:443
        timeout-minutes: 5
